# Copy this file to .env and fill in your values
# In Railway, set these as environment variables in the service dashboard

# PostgreSQL connection string (provided automatically by Railway Postgres plugin)
DATABASE_URL="postgresql://user:password@localhost:5432/nutriapp?schema=public"

# Server port (Railway sets PORT automatically; defaults to 3000)
PORT=3000
